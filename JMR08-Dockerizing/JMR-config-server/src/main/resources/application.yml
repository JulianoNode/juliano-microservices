server:
  port: 8888
spring:
  application:
    name: JMR-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JulianoNode/JMR-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'